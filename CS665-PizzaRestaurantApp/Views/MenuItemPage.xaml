<Window x:Class="CS665_PizzaRestaurantApp.Views.MenuItemPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CS665_PizzaRestaurantApp.Views"
        mc:Ignorable="d"
        Title="MenuItemPage" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Name"/>
            <TextBox x:Name="NameTextBox" Margin="0,5"/>

            <TextBlock Text="Price"/>
            <TextBox x:Name="PriceTextBox" Margin="0,5"/>

            <TextBlock Text="Description"/>
            <TextBox x:Name="DescriptionTextBox" Margin="0,5"/>

            <StackPanel Orientation="Horizontal" Margin="0,5">
                <Button Content="Add" Width="75" Click="AddButton_Click"/>
                <Button Content="Update" Width="75" Margin="5,0,0,0" Click="UpdateButton_Click"/>
                <Button Content="Delete" Width="75" Margin="5,0,0,0" Click="DeleteButton_Click"/>
            </StackPanel>
        </StackPanel>

        <ListBox x:Name="MenuItemListBox" Grid.Row="1" DisplayMemberPath="Name" 
                 SelectionChanged="MenuItemListBox_SelectionChanged"/>
    </Grid>
</Window>
