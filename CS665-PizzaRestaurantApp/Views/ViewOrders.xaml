<Window x:Class="CS665_PizzaRestaurantApp.Views.ViewOrders"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CS665_PizzaRestaurantApp.Views"
        mc:Ignorable="d"
        Title="Customer Order History" Height="700" Width="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Customers with Orders -->
        <GroupBox Header="Customers">
            <DataGrid x:Name="CustomersDataGrid" AutoGenerateColumns="False" 
                      SelectionChanged="CustomersDataGrid_SelectionChanged"
                      IsReadOnly="True" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding CustomerID}" Width="Auto"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                    <DataGridTextColumn Header="Orders" Binding="{Binding OrderCount}" Width="Auto"/>
                    <DataGridTextColumn Header="Total Spent" Binding="{Binding TotalSpent, StringFormat=C}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Customer's Orders -->
        <GroupBox Header="Customer's Orders" Grid.Row="1" Margin="0,10,0,0">
            <DataGrid x:Name="OrdersDataGrid" AutoGenerateColumns="False" 
                      SelectionChanged="OrdersDataGrid_SelectionChanged"
                      IsReadOnly="True" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Order ID" Binding="{Binding OrderID}" Width="Auto"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding OrderDate, StringFormat=MM/dd/yyyy}" Width="Auto"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=C}" Width="Auto"/>
                    <DataGridTemplateColumn Header="Actions" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" Click="EditOrder_Click" Margin="2" Padding="5,2"
                                            Tag="{Binding OrderID}"/>
                                    <Button Content="Delete" Click="DeleteOrder_Click" Margin="2" Padding="5,2"
                                            Tag="{Binding OrderID}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Order Items -->
        <GroupBox Header="Order Items" Grid.Row="2" Margin="0,10,0,0">
            <DataGrid x:Name="OrderItemsDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Image" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding ImagePath}" Height="60" Stretch="Uniform"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Item" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="Auto"/>
                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="Auto"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding LineTotal, StringFormat=C}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Window>