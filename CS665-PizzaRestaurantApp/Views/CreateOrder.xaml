<Window x:Class="CS665_PizzaRestaurantApp.CreateOrder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CS665_PizzaRestaurantApp"
        mc:Ignorable="d"
        Title="CreateOrder" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Customer Selection -->
        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="0">
            <TextBlock Text="Customer:" Width="100" VerticalAlignment="Center"/>
            <ComboBox x:Name="CustomerComboBox" Width="250"
                      DisplayMemberPath="Name"
                      SelectedValuePath="CustomerID"/>
        </StackPanel>

        <!-- Menu Item Selection -->
        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="1">
            <TextBlock Text="Menu Item:" Width="100" VerticalAlignment="Center"/>
            <ComboBox x:Name="MenuItemComboBox" Width="250"
                      DisplayMemberPath="Name"
                      SelectedValuePath="ItemID"
                      SelectionChanged="MenuItemComboBox_SelectionChanged"/>
        </StackPanel>

        <!-- Quantity -->
        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="2">
            <TextBlock Text="Quantity:" Width="100" VerticalAlignment="Center"/>
            <TextBox x:Name="QuantityTextBox" Width="250" TextChanged="QuantityTextBox_TextChanged"/>
        </StackPanel>

        <!-- Total -->
        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="3">
            <TextBlock Text="Total:" Width="100" VerticalAlignment="Center"/>
            <TextBlock x:Name="TotalTextBlock" Width="250"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10" Grid.Row="4">
            <Button Content="Create Order" Click="CreateOrderButton_Click" Width="120" Margin="0,0,10,0"/>
            <Button Content="Cancel" Click="CancelButton_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
