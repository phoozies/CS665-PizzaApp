<Window x:Class="CS665_PizzaRestaurantApp.CreateOrder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CS665_PizzaRestaurantApp"
        mc:Ignorable="d"
        Title="Create New Order" Height="600" Width="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Customer Selection -->
        <GroupBox Header="Customer" Grid.Row="0" Margin="0,0,0,10">
            <ComboBox x:Name="CustomerComboBox" 
                      DisplayMemberPath="Name"
                      SelectedValuePath="CustomerID"
                      FontSize="14" Height="30"/>
        </GroupBox>

        <!-- Menu Items Grid -->
        <GroupBox Header="Menu Items" Grid.Row="1" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="MenuItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="LightGray" BorderThickness="1" Margin="5" 
                                    Width="150" Height="180" CornerRadius="5">
                                <StackPanel Orientation="Vertical">
                                    <Image Source="{Binding ImagePath}" Height="100" Stretch="Uniform"
                                           MouseDown="MenuItemImage_MouseDown" Tag="{Binding ItemID}"
                                           Cursor="Hand" ToolTip="Click to select"/>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="5,0" 
                                               TextAlignment="Center"/>
                                    <TextBlock Text="{Binding Price, StringFormat=C}" Margin="5,0" 
                                               TextAlignment="Center" Foreground="Green"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <!-- Selected Item and Quantity -->
        <GroupBox Header="Order Details" Grid.Row="2" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Selected Item:" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock x:Name="SelectedItemText" Grid.Column="1" VerticalAlignment="Center" FontWeight="Bold"/>

                <TextBlock Text="Quantity:" Grid.Column="2" VerticalAlignment="Center" Margin="10,0,10,0"/>
                <TextBox x:Name="QuantityTextBox" Grid.Column="3" Width="50" VerticalAlignment="Center"
                         TextChanged="QuantityTextBox_TextChanged" Text="1"/>
            </Grid>
        </GroupBox>

        <!-- Total and Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right">
            <TextBlock Text="Total:" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock x:Name="TotalTextBlock" Text="$0.00" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" 
                       MinWidth="100" Margin="0,0,20,0"/>
            <Button Content="Create Order" Click="CreateOrderButton_Click" Width="120" Height="30" 
                    Margin="0,0,10,0" FontWeight="Bold"/>
            <Button Content="Cancel" Click="CancelButton_Click" Width="80" Height="30"/>
        </StackPanel>
    </Grid>
</Window>